// jest-dom enhances Jest matchers for DOM assertions.
// It offers additional matchers like `toHaveTextContent` that allow assertions on DOM elements.
// With jest-dom, you can assert things like:
// expect(element).toHaveTextContent(/react/i)
// For more information, check out: https://github.com/testing-library/jest-dom


import '@testing-library/jest-dom';
